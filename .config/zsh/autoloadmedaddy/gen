#!/bin/sh
function gen () {
s=''; e='';
while getopts 'sel:' o; do
  case ${o} in
    l) l=${OPTARG};;
    s) unset s;;
    e) unset e;;
    esac
done
keepassxc-cli generate -Uln ${s=-s} ${e=-e} -L${l:-64} | xclip -selection clipboard
}